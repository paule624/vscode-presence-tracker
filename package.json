{
  "name": "vscode-presence-tracker",
  "displayName": "vscode-presence-tracker",
  "description": "Synchronise le statut VS Code avec un portfolio",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": ["onStartupFinished", "onDidOpenTextDocument"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscode-presence-tracker.helloWorld",
        "title": "Hello World"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.96.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0"
  },
  "dependencies": {
    "axios": "^1.7.9"
  }
}
